using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CatPaw : MonoBehaviour
{
    [SerializeField]
    float minAngle = -50, maxAngle = -20;

    float newAngle;
    bool isUp;

    void Start()
    {
        newAngle = minAngle;
    }

     
    void PawWave()
    {
        
    }

    async void ToAngle()
    {
        while(transform.rotation.eulerAngles.z != )
    }
}
